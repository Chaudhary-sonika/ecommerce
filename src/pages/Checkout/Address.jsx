import { useAddress } from "../../Contexts/AddressContext"


export const Address = ()=>{
    const {addressState, addressDispatch, addAddress, removeAddress, editAddresss, setAddressInitialState, resetAddress} = useAddress();
    return(
        <>
        
        </>
    )
}